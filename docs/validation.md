# Проверка перед демонстрацией

Результаты на машине разработки, 23 сентября 2026 года.

| Проверка | Результат |
|---|---|
| Автономные тесты | 75 passed |
| Установка в отдельное пустое окружение по uv.lock | Успешна; 75 тестов прошли в чистой копии исходников без .env |
| Первый запуск с пустым кэшем модели | Все пять демо прошли; загрузка и прогрев 146,70 с, запросы с брифом 0,367–0,483 с |
| 448 комбинаций города, категории, формата и даты без брифа | 0 нарушений фильтров, 0 одинаковых объяснений внутри выдачи |
| Пять сценариев с настоящими локальными эмбеддингами | Все ожидаемые исходы подтверждены |
| Повтор одинакового запроса | Порядок карточек совпадает |
| Смена 13 октября на 31 декабря | Другой состав карточек, причина занятости показана |
| Время после прогрева, без внешней LLM | Около 0,12–0,16 с для сценариев с брифом |
| OpenAI gpt-4o, одиночный запрос без кэша | Валидный выбор фактов за 1,939 с при разрешённом таймауте 5 с |
| NVIDIA, одиночный запрос | AuthenticationError; нужна проверка ключа/активации |
| Полный прогон с внешними API | Пока не подтверждён |
| Визуальный осмотр браузера | Не выполнен: нет разрешения Computer Use |
| GitHub Actions на Linux | Задача не стартовала: GitHub сообщил «account is locked due to a billing issue» |

Воспроизводимый запуск: `uv run --frozen --extra dev python run.py`.
Полная автономная проверка: `uv run --frozen --extra dev python run.py --check`.
`uv.lock` фиксирует зависимости; Linux/Windows используют CPU-вариант PyTorch.
Workflow для Linux настроен без API-ключей и без скачивания модели в тестах.
Первый запуск остановлен инфраструктурой до выполнения шагов:
https://github.com/BAITC-Hacks/hack-22572782-neuroforge/actions/runs/35860191121
Это не подтверждает прохождение тестов на Linux; нужно снять billing-блокировку
со стороны владельца GitHub-аккаунта/организации и повторить CI.

Первый вызов OpenAI с лимитом 2 секунды завершился APITimeoutError.
После живой проверки значения по умолчанию увеличены до 4 секунд на провайдера
и 5 секунд общего ожидания объяснений. Это оставляет время для локального
ранжирования в ориентире 10 секунд, но не заменяет замер полного запроса.

Поле trace.explanation_sources и раскрывающаяся секция интерфейса показывают,
использовалась ли LLM, кэш или резервные факты. Успешный HTTP-ответ с fallback
не считается доказательством работоспособности внешнего провайдера.

Приложение использует ключи из локального .env. Сумма доступных кредитов,
остаток и привязка ключей к хакатонным организациям через эти проверки не
подтверждались. API-ключи и ответы с данными авторизации в репозиторий не включены.
