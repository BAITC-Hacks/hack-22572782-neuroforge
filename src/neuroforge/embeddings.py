"""
Локальные мультиязычные эмбеддинги (sentence-transformers) для семантического
сходства между контекстом запроса и описанием подрядчика. Считается локально —
без сетевой зависимости на критичном (ранжирующем) пути, детерминированно.

TODO: encode(), cosine_similarity(), кэш в data/processed/embeddings_cache.npz
(build_embeddings.py пересчитывает кэш при изменении датасета).
"""
