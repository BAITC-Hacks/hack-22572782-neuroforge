"""
Обёртка над Anthropic API для генерации текста объяснений.
temperature=0, жёсткий timeout (settings.llm_timeout_s) — при превышении
или ошибке вызывающий код (explain.py) переключается на шаблонный fallback.

TODO: реализовать вызов, обработку таймаута/ошибок.
"""
