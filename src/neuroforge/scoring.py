"""
Детерминированный скоринг выживших кандидатов + выбор top-k.

Формула — взвешенная сумма фич из config.settings.scoring (веса НЕ здесь).
Сортировка: desc(score), tie-break по profile.id asc — гарантирует
одинаковый порядок при равных скорах (требование детерминизма).

TODO: реализовать после embeddings.py.
"""
